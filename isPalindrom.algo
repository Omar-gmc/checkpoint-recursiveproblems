FUNCTION isPalindrome(str:STRING,n: INTEGER) : BOOLEAN

BEGIN

IF (str.length == 0 OR str.length == 1) THEN
    RETURN TRUE;
ELSE_IF (str(0) <>str(str.length-1)) THEN
    RETURN FALSE;
ELSE
    RETURN isPalindrome(str,n+1)
END_IF
END
    